status: 200
message: The leaderboard was retrieved.
data:
  type: object
  properties:
    total:
      type: integer
    leaderboard:
      type: array
      items:
        type: object
        properties:
          id:
            type: string
          name:
            type: string
          score:
            type: integer
        required: ['id', 'name', 'score']
  required: ['total', 'leaderboard']
